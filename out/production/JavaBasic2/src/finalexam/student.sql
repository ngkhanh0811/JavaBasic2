use bookstore;
create table Student(
                        StudentID varchar(50),
                        StudentName varchar(50),
                        Address varchar(50),
                        Phone varchar(50)
);